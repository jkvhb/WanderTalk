# WanderTalk 产品需求文档 v3.0

> 文档日期：2026-07-17  
> 文档状态：产品方向已确认，作为后续阶段规划基线  
> 前置版本：`PRD_v2.html`（2026-06-06）  
> 当前产品阶段：固定 318 旗舰作品与传播验证

---

## 一、版本说明

PRD v2 将产品定义为“在地图上规划自驾路线，自动生成带旁白讲解的飞行动画视频”。经过 Phase 1～4e 的实际开发与多轮手测，产品已经从单纯的视频生成工具演进为：

> **以真实地图和可靠攻略为骨架，通过 AI 完成规划、素材组织和视觉叙事的旅行产品。**

本版 PRD 重点解决三件事：

1. 明确长期产品身份，不再把“视频”视为唯一交付物；
2. 将当前传播验证版本与未来正式产品拆开，避免过度建设；
3. 将开发过程中已经确认的自动配图、动效编排、互动章节、离线生成、本地会话记忆和行后 Vlog 方向纳入统一规划。

本版不否定 PRD v2 已完成的路线规划、旁白、地图动画与视频导出目标；它重新安排这些能力在产品中的关系和优先级。

---

## 二、产品定位

### 2.1 第一身份

WanderTalk 是一个 **AI 旅行策划与视觉叙事产品**。

用户可以从一句自然语言需求开始，经过 AI 主动追问、真实路线规划和内容确认，获得：

- 可编辑的结构化旅行方案；
- 基于真实地图的动画化攻略；
- 可通过链接观看的章节式互动作品；
- 可选的视频导出；
- 长期可扩展为旅行中的随行记录和旅行后的自动 Vlog。

### 2.2 一句话描述

> 说出你想去哪里，WanderTalk 帮你规划真实可行的旅程，并把它讲成一部可以点击继续的旅行故事。

### 2.3 核心差异化

| 能力 | 普通攻略生成器 | 普通地图规划 | 普通视频生成器 | WanderTalk |
|---|---|---|---|---|
| 个性化需求追问 | 部分 | 无 | 部分 | 主动追问并结构化保存 |
| 真实路线与空间关系 | 弱 | 强 | 弱 | 强，真实地图是产品骨架 |
| 攻略可信度 | 容易依赖模型记忆 | 路线可信、内容少 | 通常不负责事实 | 动态信息联网核验并保留来源 |
| 视觉叙事 | 以文字为主 | 很弱 | 强但难编辑 | 地图、讲解、照片和动效统一编排 |
| 交付形式 | 文本页面 | 地图路线 | 线性视频 | 互动网页为长期方向，视频为可选导出 |
| 中途恢复 | 通常依赖云会话 | 保存路线 | 任务式 | 会话、需求、成果和任务检查点共同恢复 |
| 旅行生命周期 | 行前 | 行前/行中 | 行后 | 长期覆盖行前、行中、行后 |

### 2.4 当前不做运营扩张

当前阶段只打磨产品，不建设：

- 旅行社区；
- 创作者商业化；
- 模板市场；
- 多人协作与组织管理；
- 酒店、门票和餐饮交易；
- 广告、会员和复杂增长体系。

---

## 三、目标用户与核心任务

### 3.1 主要用户

有大致目的地、旅行天数或模糊想法，但不擅长规划，也不会制作视觉作品的普通自驾旅行者。

典型表达：

- “帮我规划一趟 9 天的 318 自驾。”
- “我们带父母出行，不想每天开太久。”
- “想看雪山和湖泊，住宿尽量舒服。”
- “我不知道哪些地方值得停，希望你帮我决定。”

### 3.2 次要用户

- 旅行博主：快速制作路线讲解素材；
- 车队领队：向成员展示每日路线和注意事项；
- 旅行回忆者：根据实际素材生成旅行 Vlog。

当前不为次要用户单独建设协作、商业发布或团队权限能力。

### 3.3 用户核心任务

1. 将模糊旅行想法变成真实可行的每日路线；
2. 理解每天怎么走、为什么这样走、会看到什么；
3. 用比长篇文字更轻松的方式预览整趟旅程；
4. 中途离开后继续对话和制作，不重复描述与生成；
5. 通过网页链接向同行者或朋友展示旅行计划；
6. 在未来将真实旅行素材转化为 Vlog 和回忆作品。

---

## 四、产品阶段策略

### 4.1 当前阶段：固定 318 传播验证版

当前核心目的不是向任意用户交付完整 SaaS，而是制作一份足够完整、易传播、能让别人迅速理解产品价值的固定 318 旗舰作品。

当前作品必须：

- 使用固定的 318 真实路线和已验证内容；
- 通过公开网页链接直接观看；
- 不要求观看者登录、配置 API Key 或安装应用；
- 一天作为一个章节；
- 每章播放预先渲染好的动画视频；
- 章尾等待用户点击“继续旅程”；
- 支持“重看上一章”；
- 支持自动连续播放；
- 自动播放模式可用于录制或导出视频；
- 桌面端和手机端均可顺畅体验。

### 4.2 当前架构选择：视频优先的互动网页外壳

当前采用：

```text
固定 318 作品数据
      ↓
按天预渲染章节视频
      ↓
互动网页外壳
  ├─ 第 1 天视频
  ├─ 章节门
  ├─ 第 2 天视频
  ├─ 章节门
  └─ ……
```

采用该方案的原因：

- 最快形成可传播链接；
- 复用现有地图、配音、图片和动画能力；
- 各设备播放结果一致；
- 能直接验证章节节奏和视觉吸引力；
- 不提前建设复杂的通用互动渲染器。

### 4.3 长期架构：原生互动网页引擎

长期保留并记录“互动网页引擎优先”方向：

- MapLibre 真实地图在作品页中实时运行；
- 章节内地图、图片、文字和动效由网页实时渲染；
- 用户可查看地点详情和地图空间关系；
- 互动模式和自动播放模式复用同一时间轴；
- 视频只是自动播放模式的录制结果。

当前阶段不实施该引擎，但当前章节清单与作品数据必须为未来替换章节渲染器保留兼容边界。

---

## 五、固定 318 旗舰作品体验

### 5.1 作品结构

- 整趟路线由若干“日章节”组成；
- 默认一天一个章节；
- 每个日章节包含多个真实路线节点和内部场景；
- 章节标题以“日期/第几天＋起终点＋主题”表达；
- 章节间存在明确的等待与继续动作；
- 作品提供弱化的章节目录，用于查看已解锁章节。

### 5.2 章节状态

每个章节播放器包含以下确定状态：

1. `ready`：封面和“开始旅程”；
2. `playing`：章节视频与声音播放中；
3. `paused`：用户主动暂停；
4. `awaiting_continue`：章节播放结束，等待用户操作；
5. `replaying_previous`：临时重播上一章；
6. `autoplay_wait`：自动模式的章间停顿；
7. `completed`：整趟作品结束；
8. `error`：当前媒体失败，提供重试或降级。

### 5.3 章节门交互

章节结束时：

- 主按钮“继续旅程 →”进行克制但明显的呼吸或轻跳；
- 次按钮“← 重看上一章”保持安静，不与主按钮争抢注意力；
- 第一章不显示“重看上一章”；
- 点击“重看上一章”后，上一章从头完整播放；
- 重播结束后回到用户发起回看时的章节门，不改变当前作品进度；
- 用户可以通过章节目录回看已经解锁的任意章节；
- 未解锁章节不允许直接跳转，保证首次观看的叙事顺序。

### 5.4 自动播放与视频导出

自动播放不是另一套作品逻辑，而是系统代替用户触发章节门：

- 每章结束后默认停留 2.5 秒；
- 作品配置可在 1.5～4 秒范围内调整停留；
- 停留结束后自动触发“继续旅程”；
- 自动模式仍使用同一章节顺序、视频素材和转场；
- 导出视频时隐藏交互按钮；
- 视频导出保留片头、日章节标题、章间停顿和片尾；
- 初始导出格式沿用 WebM，后续可增加服务端 MP4 转码。

### 5.5 移动端行为

- 首次进入必须由用户点击“开始旅程”，用于解锁移动浏览器音频播放权限；
- 之后章节可自动衔接或由用户继续；
- 页面使用横竖屏自适应布局，不强制用户旋转设备；
- 手机端不展示过密的地图标签和长文本；
- 操作按钮位于单手可触达区域；
- 网络较慢时先加载章节封面和音频提示，视频完成缓冲后再开始。

### 5.6 公开链接

- 固定 318 作品拥有稳定公开地址；
- 页面只包含冻结后的作品数据和媒体，不包含任何服务端或用户 API Key；
- 观看过程中不调用 LLM、搜图或视频生成接口；
- 链接可以被微信、浏览器和社交平台直接打开；
- 页面提供清晰但不打断体验的“创建我的旅行”入口；
- 当前只发布固定 318 作品，不开放任意用户作品一键发布。

---

## 六、视觉叙事系统

### 6.1 基本原则

视觉主题不绑定纸雕、手账或某一种固定风格。系统将“事实内容”和“视觉表达”解耦：

- 真实地图负责空间与路线；
- 真实照片/视频负责景色可信度；
- 程序化动画负责稳定节奏；
- 生成式内容只负责重点桥接、环境氛围和非事实层；
- 文字、地名、字幕和关键数据由程序渲染。

### 6.2 统一美术方向

- 一趟旅行只有一个总美术方向；
- 每天可根据地貌、天气、讲解内容调整色调和节奏；
- 日章节变化必须服从全局字体、构图、动效词汇和材质规则；
- AI 可以自主决定每日变化，但不得令作品产生“每天换一套产品”的割裂感；
- 同一作品、同一配置、同一素材必须确定性重放。

### 6.3 程序化为主＋重点节点离线生成

视觉生成采用已确认的混合策略：

- 地图、小车、路线进度、数据标签和常规转场程序化渲染；
- AI 根据讲解生成受约束的分镜和动效配置；
- 只为重点节点离线生成 2～4 秒桥接视频或动态镜头；
- 全部生成在播放前完成并缓存；
- 生成失败时回退到程序化转场；
- 不一次性生成整条长视频；
- 修改某个节点时只重做相关片段。

### 6.4 首个视觉样章

样章选择：**康定—折多山—新都桥**。

选择原因：

- 同时包含城市、山路、高海拔和强景观节点；
- 真实路线与景色辨识度高；
- 能验证地图到照片的衔接；
- 能验证统一风格下的冷暖和节奏变化；
- 容易暴露镜头、地形、素材和叙事割裂问题。

样章必须制作两种美术方向供实测比较，完整 318 作品只能在样章通过后展开。

### 6.5 样章验收标准

1. 用户始终能判断自己位于真实路线的什么位置；
2. 地图、真实照片、讲解与装饰动效层级清楚；
3. 章节结束后，用户自然注意到并愿意点击“继续旅程”；
4. “重看上一章”不会破坏当前进度；
5. 同一章节重复播放结果一致；
6. 手机端画面、文字和按钮均清楚；
7. 地形镜头不穿地、不闪切、不出现长帧卡顿；
8. 删除讲解文字后，视觉仍能表达当天路线推进；
9. 真实景点不会被生成画面误导或替换；
10. 未通过样章实测时，不开始全量 318 章节制作。

---

## 七、未来通用旅行规划流程

### 7.1 对话入口

用户通过一句话发起项目，例如：

> “帮我规划一趟 9 天的 318 自驾，我们有老人，不想每天开太久。”

AI 将需求解析为结构化字段，并主动追问真正影响路线的缺失条件，例如：

- 出发日期与总天数；
- 出发地、目的地与返程方式；
- 同行人结构；
- 每日最大驾驶时长；
- 住宿舒适度和预算；
- 景观、文化、美食等偏好；
- 高原适应和身体限制；
- 必去、避开和可调整地点。

### 7.2 确认点策略

开发验证期保留三个确认点：

1. 需求确认；
2. 行程确认；
3. 作品大纲确认。

第三个确认点用于观察和修正 AI 对叙事重点、视觉节奏和素材分配的判断。

正式上线后砍掉第三个确认点：用户确认行程后，AI 自主完成作品大纲并进入后台制作。用户仍可在生成后局部修改某一天并重新制作该章节。

### 7.3 AI 分章

- 通用产品由 AI 根据实际规划决定章节结构；
- 默认一天一个章节；
- 一天内可以包含多个地点和场景；
- AI 可以建议将内容特别密集的一天拆为上下篇，但必须保持同一天归属；
- 不允许未经用户确认将不同日期合并为同一日章节；
- 用户未来可以拆分、合并同日子章节、改名和调整重点。

### 7.4 后台制作过程

用户确认行程后，系统展示清晰的后台阶段：

```text
正在核验路线与动态信息
→ 正在整理每日讲解
→ 正在搜索真实素材
→ 正在生成配音
→ 正在设计章节动效
→ 正在生成重点转场
→ 正在检查作品质量
→ 正在生成分享作品
```

用户看到的是可理解的阶段状态，而不是底层模型、队列和接口细节。
### 7.5 AI 规划编排与工具边界

长期的通用规划能力不由模型一次性生成整份作品，而由“对话 AI＋受约束工具＋确定性校验器”共同完成。

对话 AI 负责：

- 理解用户当前意图并决定是否继续追问；
- 根据工作流状态选择地点、路线、核验、内容和制作工具；
- 综合工具结果形成候选方案；
- 在校验失败时只修复受影响部分，不重复生成全部成果。

工具负责可靠执行，至少分为：

- 地点工具：返回稳定地点 ID、规范名称、坐标和地点类型；
- 路线工具：返回真实道路、距离、耗时和途经关系；
- 规划校验器：检查重复地点、零距离路段、跨日断裂、日程超限和支线误入主线；
- 事实核验工具：核验动态信息并保留来源、时间和适用范围；
- 内容工具：生成攻略、旁白、素材检索词和章节大纲；
- 制作工具：生成配音、动效、地图动画、章节媒体和分享作品；
- 质量工具：检查事实、素材、音画、章节完整性和降级结果。

路线点、讲解点、住宿点、跨日起点和可选支线必须在数据层明确区分。同一真实地点使用同一 `placeId`；前一日终点可以作为下一日起点继续参与路线计算，但不得因此重复讲解或生成零距离飞行动画。

通用规划采用以下闭环：

```text
结构化需求
→ 生成候选行程
→ 调用地点与路线工具
→ 确定性校验
→ 局部修复或向用户追问
→ 用户确认行程
→ 生成攻略与作品
→ 作品质量检查
→ 保存并交付
```

模型微调不是保证路线合法性的手段。近期优先积累工具契约、校验规则、来源数据和用户修改记录；未来即使使用微调学习规划偏好与文案风格，确定性校验仍是发布前硬门槛。

### 7.6 用户线索、地点核验与主动澄清

用户输入的地名、行政区、海拔、道路名称和行程描述均视为**待核验线索**，不能直接作为事实写入路线。用户可能使用俗称、旧称、错别字、相邻行政区名称或仅描述一个没有正式名称的路口；系统不得因为文字看起来明确就跳过核验。

对话 AI 的系统提示词必须明确以下原则：

- 先判断输入属于正式 POI、自然地貌、行政区、道路节点、俗称还是历史名称；
- 地名与行政区冲突时，扩大到相邻县市和上级行政区检索，不能直接判定地点不存在；
- 依次尝试正式名称、别名、简称、道路关系和邻近地标，并综合地点、道路与公开资料；
- 不得猜测坐标、用节点直线替代真实道路或用相似景点和图片补位；
- 存在多个合理候选或证据不足时，暂停受影响部分并向用户提出具体、可回答的澄清问题；
- 用户熟悉的原始称呼与系统核验后的规范名称同时保存，不擅自抹去用户语境；
- 地点未通过核验前，不得进入路线计算、攻略生成、素材匹配、播报和动画制作。

地点核验状态采用以下闭环：

```text
用户线索
→ 地点类型判断
→ 多策略检索
→ 候选地点与证据
→ 置信度判断
→ 自动确认 / 向用户澄清
→ 规范地点入库
→ 路线与内容生产
```

每个规范地点至少保存：用户原始名称、规范展示名称、别名或旧称、地点类型、稳定地点 ID、坐标及坐标系、行政区、证据来源、核验时间、置信度和用户确认状态。系统提示词负责规定判断与追问原则，专业工具负责提供证据，确定性校验器负责阻止不合格地点进入后续流程；三者缺一不可。

固定 318 底稿是该流程的第一套监督样本。类似“营官寨三岔路口”这类无正式 POI 的道路节点，应记录用户称呼，但以真实道路拓扑和规范描述入库。近期不训练专用模型；先积累“原始输入—检索证据—规范结论—用户修正”的真实案例，未来再评估使用微调学习地点消歧、规划偏好和内容风格。即使完成微调，工具核验和发布前硬校验仍然保留。

---

## 八、本地会话记忆与成果恢复

### 8.1 目标

实现类似持续对话的本地恢复：用户今天聊到一半，明天可以继续，不需要重新描述需求，也不需要重新发送全部历史造成 token 浪费。

近期只保证同一浏览器、同一设备恢复；云端账号与跨设备同步后续再做。

### 8.2 保存内容

- 完整聊天消息；
- 滚动上下文摘要；
- 已确认需求；
- 未决问题；
- 关键产品决定；
- 当前路线方案与版本；
- 每日攻略与旁白；
- 图片、音频、动效配置和生成片段索引；
- 后台任务状态与错误；
- 最后浏览位置和当前确认步骤。

### 8.3 token 节省策略

恢复对话时不默认向模型重发完整历史，而是组合：

```text
结构化旅行需求
+ 已确认决定
+ 滚动摘要
+ 未决问题
+ 最近少量消息
+ 当前任务所需的相关历史片段
```

完整历史只用于用户查看、重新总结或定位争议，不作为每次请求的固定上下文。

### 8.4 自动保存触发

- 每条用户或 AI 消息完成后；
- 每次需求字段变化后；
- 每次确认点完成后；
- 每个后台任务状态变化后；
- 每项素材生成成功后；
- 页面隐藏、刷新或关闭前；
- 固定时间间隔内存在未保存变化时。

### 8.5 幂等与局部失效

- 文本、参数和模型版本共同形成内容哈希；
- 输入未变化时复用既有成果；
- 修改某一天只让该日章节及其明确依赖失效；
- 搜图、TTS、动效和视频片段可以独立重试；
- 已成功任务不因页面关闭而被标记失败；
- 恢复时从最后一个可靠检查点继续。

---

## 九、攻略真实性与可追溯性

### 9.1 硬性原则

- 路线、地图、地名、距离和海拔不得由生成模型伪造；
- 住宿、餐饮、开放状态、道路风险等动态信息必须联网核验；
- 动态信息保存来源 URL、抓取时间和适用日期；
- 事实、模型推断和个性化建议在数据层明确区分；
- 多来源冲突时向用户提示，不擅自选择对产品最有利的答案；
- 无法确认的信息标记为“不确定”；
- 发布作品保存当时使用的信息快照。

### 9.2 素材真实性

- 景点主体优先使用可追溯的真实照片或视频；
- 搜图结果根据标题、简介、标签、地点和视觉模型进行多层验证；
- 用户可以替换错误或不满意素材；
- 无法获得可靠素材时降级为地图＋文字；
- AI 生成素材不得冒充真实景点记录；
- 图片、视频和生成素材分别保存来源、授权和生成信息。

### 9.3 文本与图片协同

正式产品允许文本和素材动态协同：

- 生成讲解时同步规划需要哪些视觉证据；
- 搜不到可靠图片时，先扩大或调整检索策略；
- 若文字描述过度具体且无可靠素材，允许修改表达以匹配可证实内容；
- 不允许为了找到漂亮图片而篡改真实路线和事实；
- 用户只看到“正在搜索/正在核验”，底层可以执行多轮检索和局部改写。

---

## 十、作品数据边界

当前视频章节和未来原生互动章节共享统一作品清单。

### 10.1 作品

- 作品 ID 与版本；
- 标题、简介和封面；
- 全局美术方向；
- 真实路线摘要；
- 章节顺序；
- 分享状态；
- 自动播放配置；
- 数据与素材版本快照。

### 10.2 日章节

- 日期/第几天；
- 起点、终点和途经地点；
- 真实路线范围；
- 章节标题和主题；
- 视频章节地址；
- 未来互动渲染配置；
- 封面、摘要和旁白；
- 上一章/下一章关系；
- 预加载资源；
- 生成状态和内容哈希。

### 10.3 内部场景

- 对应真实节点；
- 地图镜头；
- 图片和视频；
- 字幕和讲解时间窗；
- 程序化动效配置；
- 生成式桥接片段；
- 事实来源和素材来源。

该边界保证未来可以将某章从“预渲染视频”替换为“原生互动渲染”，而不改变分享页、章节导航和作品结构。

---

## 十一、非功能需求

### 11.1 稳定性

- 同一作品重复播放结果一致；
- 视频、音频和章节状态不能因页面切后台而永久错位；
- 单章加载失败不导致整个作品崩溃；
- 地形服务失败时降级到平面地图；
- 生成式转场失败时降级到程序化转场；
- 作品页不依赖制作端 API Key。

### 11.2 性能

- 首屏只加载封面、作品摘要和第一章必要资源；
- 播放当前章时预加载下一章；
- 不一次性下载全程高清视频；
- 手机端根据网络和设备能力选择适当码率；
- 路线计算、搜图和生成成果优先复用缓存；
- 长路线按章节拆分，避免整条作品一次性占用大量内存。

### 11.3 可访问性与可用性

- 核心按钮支持键盘操作；
- 字幕可以开启或关闭；
- 关键数据不只通过颜色表达；
- 动效强度遵循系统“减少动态效果”偏好；
- 视频无法播放时提供章节文字和静态地图摘要；
- 用户始终能判断当前第几天、路线位置和下一步操作。

### 11.4 隐私

- 当前会话和项目默认仅存本地；
- 清除项目时同时清除关联摘要、任务和素材索引；
- 固定公开 318 作品不包含个人数据；
- 未来用户作品发布前必须明确选择访问范围；
- 行中照片、轨迹和定位信息默认私密。

---

## 十二、当前版本验收标准

固定 318 传播验证版达到以下条件才视为完成：

1. 通过公开链接打开，无需登录和 API Key；
2. 首次点击后可以正常播放声音；
3. 一天对应一个章节；
4. 每章结束停在章节门；
5. “继续旅程”视觉明显但不过度；
6. “重看上一章”完整重播且不改变当前进度；
7. 自动模式可以完整播放全程；
8. 自动模式可用于导出连续视频；
9. 桌面端和主流手机浏览器可观看；
10. 真实地图、路线、景点和图片没有明显错配；
11. 当前章播放时下一章完成预加载；
12. 任意单章媒体失败时提供重试或静态降级；
13. 观看者在第一章结束前能够理解产品是“AI旅行规划＋视觉叙事”，而非普通旅游宣传片；
14. 完整作品制作前，康定—折多山—新都桥样章已通过人工体验验证。

当前阶段关注产品理解度、完成体验和主动分享意愿，不建设复杂运营看板。

---

## 十三、开发路线图

### 阶段 A：现有能力稳定

- 完成地图、3D 地形和镜头安全修复；
- 消除长帧卡顿和资源加载中断；
- 验证自动配图和动效编排整链生效；
- 固化当前 318 路线、旁白和素材基线。

### 阶段 B：视觉样章

- 制作康定—折多山—新都桥完整日章节；
- 设计两种美术方向；
- 加入章节门、回看和自动模式；
- 完成桌面端与手机端体验验证；
- 通过样章验收后选定全局视觉方向。

### 阶段 C：固定 318 旗舰作品

- 按天完成全部章节；
- 统一章节封面、标题、节奏和片尾；
- 建立章节视频与作品清单；
- 完成逐章预加载和错误降级；
- 生成固定公开分享链接。

### 阶段 D：自动播放与视频导出

- 由系统自动触发章节继续；
- 捕获完整自动播放过程；
- 音视频合流；
- WebM 导出、进度和下载；
- 保留逐帧录制降级路径；
- 后续增加 MP4 转码。

### 阶段 E：通用行前规划产品

- 对话式需求追问；
- 本地会话记忆和滚动摘要；
- 真实信息联网核验；
- AI 生成每日路线、攻略和章节大纲；
- 通用作品生成任务队列；
- 用户局部修改与局部重做。

### 阶段 F：原生互动网页引擎

- 真实地图实时交互；
- 网页实时渲染章节；
- 互动与自动模式共用时间轴；
- 任意用户作品一键发布链接；
- 作品版本、访问权限和撤销发布。

### 阶段 G：行中记录与行后 Vlog

- 用户补充照片、视频、语音和真实轨迹；
- 根据 EXIF、时间和位置自动归档到日章节；
- 对比规划路线与实际路线；
- 自动识别高光片段；
- 生成 Vlog 脚本、字幕、音乐和剪辑；
- 支持按天重做和素材替换；
- 地图退居片头、章节定位和路线过渡，不作为 Vlog 主画面。

---

## 十四、长期产品愿景

### 14.1 行前—行中—行后

```text
行前：想象与规划
  AI 追问 → 真实路线 → 动画化攻略 → 互动作品

行中：经历与记录
  实际轨迹 → 用户照片/视频/语音 → 自动归档

行后：理解与回忆
  素材筛选 → 故事重写 → 自动剪辑 Vlog → 回忆作品
```

### 14.2 行后 Vlog 定位

行后作品不应只是将地图动画重新播放一遍，而应使用用户真实素材形成更高级的回忆作品：

- 地图只负责定位和过渡；
- 用户真实照片、视频和声音是主体；
- AI 根据实际发生的事件重新组织故事；
- 原计划与实际旅程可以形成对照；
- 可生成纪录片版、轻松短视频版和私人回忆版；
- 用户拥有最终素材选择和局部重做权。

---

## 十五、未来机会池

以下方向记录为可能性，不进入当前固定 318 传播版范围：

- 使用真实海拔曲线作为作品进度条；
- 每完成一天获得路线印章，形成旅行护照；
- 展示“为什么 AI 这样规划”，提升信任而不只给结论；
- 根据季节生成不同旅行预览，并明确标记模拟内容；
- 使用环境声增强章节，例如风声、河流和车内声音；
- 行中自动整理用户素材并提示缺失章节；
- 行后展示“计划路线 vs 实际路线”的双时间线；
- 根据同一旅行素材生成不同叙事版本；
- 家人通过私密链接查看旅行进度；
- 根据道路风险、体力和天气变化生成“旅行信心提示”；
- 在不改变真实路线的前提下，为不同同行者生成不同讲解视角。

这些机会只有在核心行前产品、旗舰作品和真实规划可信度通过验证后才进入评估。

---

## 十六、明确不做

当前及近期明确不做：

- 将整个产品改成 Flipbook 式实时生成像素界面；
- 在观看过程中实时生成视频；
- 一次性生成不可拆分的整条长视频；
- 用 AI 生成地图、地名、道路和关键事实；
- 为传播样章建设账号、付费和复杂权限系统；
- 同时支持任意用户作品发布；
- 社区、关注、评论和内容运营；
- 酒店、门票、餐饮和车辆预订交易；
- 正式导航替代品；
- 为追求视觉效果牺牲路线真实性；
- 样章未通过前批量制作全部 318 章节。

---

## 十七、主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 视觉概念好，但实际效果普通 | 无法形成传播 | 先做折多山样章，两种方向实测后再扩展 |
| 视频外壳被误解为普通宣传片 | 产品价值不清 | 第一章明确呈现 AI 规划逻辑、真实路线和交互章节门 |
| 章节视频文件过大 | 手机加载慢 | 分章、按需加载、下一章预加载、多码率 |
| 移动端禁止自动播放声音 | 首屏无声 | 首次“开始旅程”作为用户手势解锁音频 |
| 景点图片错配 | 破坏信任 | 来源、标签、描述与视觉多层校验；允许降级文字版 |
| 生成转场失真 | 虚构真实景色 | 只用于非事实层；失败自动回退程序转场 |
| 会话很长导致 token 浪费 | 成本上升 | 结构化记忆、滚动摘要、相关历史检索 |
| 中途关闭导致重复生成 | 时间和费用浪费 | 本地检查点、内容哈希、任务幂等和局部恢复 |
| 当前视频方案未来难迁移 | 重写成本 | 统一作品/章节清单，章节渲染器保持可替换 |
| 过早扩展行中和行后 | 核心产品失焦 | 只预留数据边界，待行前产品验证后再开发 |

---

## 十八、相对 PRD v2 的主要调整

1. 产品第一身份从“视频生成工具”升级为“AI旅行策划与视觉叙事产品”；
2. 视频从唯一交付物调整为当前传播手段和未来可选导出；
3. 长期主要交付变为可分享的章节式互动网页；
4. 当前先做固定 318 旗舰作品，不立即开放通用发布；
5. 动画形态从连续线性播放扩展为“章内自动、章间用户继续”；
6. 新增“重看上一章”和自动代点击模式；
7. 新增统一美术方向与每日有限变化；
8. 新增程序化为主＋重点节点离线生成策略；
9. 新增本地会话记忆、滚动摘要和成果检查点；
10. 新增动态信息联网核验和素材可追溯要求；
11. 新增行前、行中、行后的长期生命周期；
12. 将行后自动 Vlog 作为独立高级作品，而非地图动画复刻；
13. 保留 PRD v2 的视频导出目标，但安排在旗舰互动作品之后；
14. 已提前落地的 Express 后端、DeepSeek、天地图和 Edge TTS 后端代理视为当前架构，不再要求回退到纯前端。

---

## 十九、产品决策摘要

- 核心定位：AI旅行策划与视觉叙事；
- 当前目标：固定 318 作品用于产品展示与传播；
- 当前交付：视频优先的章节式互动网页链接；
- 章节单位：一天一章，章内包含多个真实地点；
- 章节推进：章内自动、章尾等待点击；
- 辅助操作：重看上一章、已解锁章节目录、自动连续播放；
- 视频定位：自动模式的导出结果；
- 地图定位：真实路线和攻略可信度的骨架，不被风格化叙事替代；
- 视觉风格：整趟统一、每日有限变化，具体风格由样章验证；
- 生成方式：程序化为主、重点节点离线生成；
- 视觉样章：康定—折多山—新都桥；
- 需求确认：开发期三个确认点，正式上线后去掉作品大纲确认；
- 会话恢复：近期本地保存，云端同步后续；
- 可信度：动态信息联网核验、来源和时间可追溯；
- 长期架构：原生互动网页引擎；
- 长期闭环：行前规划、行中记录、行后自动 Vlog；
- 当前不涉及：运营、商业化、社区和交易。
