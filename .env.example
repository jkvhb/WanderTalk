# 318路书 大多数密钥不通过环境变量配置。
# 高德 API Key、LLM（DeepSeek）API Key 等均在应用「设置」页填写，保存在浏览器 localStorage。
#
# 例外：Phase 4d「AI 自动配图」的 Pixabay Key 必须只放服务端 .env（前端不可见，防泄露）。
# 复制本文件为 .env 并填入你的 key；免费注册：https://pixabay.com/api/docs/
PIXABAY_KEY=

# 默认 AI 模型 Kimi K2.6，仅供本地服务端读取，绝不放入浏览器或提交到 Git。
MOONSHOT_API_KEY=
